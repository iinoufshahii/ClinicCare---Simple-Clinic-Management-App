package com.example.cliniccare.ui.theme

import androidx.compose.ui.graphics.Color

/** Color definitions for the ClinicCare app theme */

/** Brand colors for the app */
val Primary = Color(0xFF06BCC1) // Teal/Cyan - Main actions, primary buttons
val PrimaryDark = Color(0xFF05A6AB) // Darker teal for hover states
val Secondary = Color(0xFF284B63) // Dark Blue - Accent elements
val TextDark = Color(0xFF353535) // Charcoal - Primary text, headings
val Background = Color(0xFFFAFAFA) // Light gray background
val Surface = Color(0xFFFFFFFF) // White - Cards and surfaces

/** Status colors for appointments and other states */
val StatusCompleted = Color(0xFF10B981) // Green
val StatusUpcoming = Color(0xFF3B82F6) // Blue
val StatusCancelled = Color(0xFF9CA3AF) // Gray
val ErrorRed = Color(0xFFEF4444) // Red

/** Light variants for backgrounds and subtle elements */
val PrimaryLight = Color(0x1A06BCC1) // 10% opacity primary
val SecondaryLight = Color(0x1A284B63) // 10% opacity secondary
